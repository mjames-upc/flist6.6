<%= simple_form_for(@tag_record) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :franken_beer %>
    <%= f.association :tag %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
